{
  plugins = {
    treesitter = {
      enable = true;
      settings = {
        # All grammars enabled by default.
        highlight = {
          enable = true;
        };
        indent = {
          enable = true;
        };
      };
    };
  };
}

